<%= form_for(@comment) do |f| %>
  <% if @comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% @comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_field :name, class: "form-control input-lg", placeholder: "Name"%>
  </div>
  <div class="field">
    <%= f.text_field :email, class: "form-control input-lg",placeholder: "Email" %>
  </div>
  <div class="field">
    <%= f.text_field :website, class: "form-control input-lg",placeholder: "Website" %>
  </div>
  <div class="field">
    <%= f.text_area :thoughts, rows: "10", class: "form-control input-lg",placeholder: "Your thoughts..." %>
  </div>
  <div class="actions">
    <%= f.submit class: "btn btn-xlarge btn-tales-one"%>
  </div>
<% end %>


